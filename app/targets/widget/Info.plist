<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>NSExtension</key>
  <dict>
    <key>NSExtensionPointIdentifier</key>
    <string>com.apple.widgetkit-extension</string>
  </dict>
  <!-- Lets the widget read the last-known location the main app's
       when-in-use permission already covers. Without this key,
       CLLocationManager.location is always nil inside the extension. -->
  <key>NSWidgetWantsLocation</key>
  <true/>
</dict>
</plist>
